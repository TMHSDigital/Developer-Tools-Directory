"""Agent-file drift checker (Phase 2 Session A core library).

Public surface is deliberately narrow: consumers should import from
submodules (``types``, ``signal``, ``semver``, ``config``, ``pragma``,
``snapshot``, ``checks.version_signal``, ``report.markdown``,
``report.json_out``, ``cli``).
"""
